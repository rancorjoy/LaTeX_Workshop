\documentclass[../main.tex]{subfiles}
\graphicspath{{\subfix{}}}

\begin{document}
\begin{tikzpicture}
\begin{axis}[
    xmin=0, xmax=2*pi,
    ymin=-1.25, ymax=1.25,
    axis x line=middle,
    axis y line=left,
    axis line style = {-{Latex}},
    xlabel=$x$,
    ylabel=$f(x)$,    
    xtick={pi/6, pi/4, pi/3, 2*pi/3, 3*pi/4, 5*pi/6, pi, 7*pi/6, 5*pi/4, 4*pi/3, 5*pi/3, 7*pi/4, 		11*pi/6, 2*pi},
    xticklabels={
    $\frac{\pi}{6}$,
    $\frac{\pi}{4}$,
    $\frac{\pi}{3}$,
    $\frac{2\pi}{3}$,
    $\frac{3\pi}{4}$,
    $\frac{5\pi}{6}$,
    $\pi$,
    $\frac{7\pi}{6}$,
    $\frac{5\pi}{4}$,
    $\frac{4\pi}{3}$,
    $\frac{5\pi}{3}$,
    $\frac{7\pi}{4}$,
    $\frac{11\pi}{6}$,
    $2\pi$},
    ytick={-1, -sqrt(3)/2, -sqrt(2)/2, -0.5, 0, 0.5, sqrt(2)/2, sqrt(3)/2, 1},     % Y-ticks
    yticklabels={
    $-1$,
    $-\frac{\sqrt{3}}{2}$, 
    $-\frac{\sqrt{2}}{2}$, 
    $-\frac{1}{2}$, $0$,
    $\frac{1}{2}$, 
    $\frac{\sqrt{2}}{2}$, 
    $\frac{\sqrt{3}}{2}$, 
    $1$},    
    width=0.8\linewidth,
    height=12cm
]
%Below the sine function is defined
\addplot [
    domain=\pgfkeysvalueof{/pgfplots/xmin}:\pgfkeysvalueof{/pgfplots/xmax}, 
    samples=100, 
    color=black,
    -{Latex} %Adds arrow to function
]
{cos(deg(x))};
%Y-Comb is defined
\addplot+ [
        ycomb,
        gray!50,                				% line color
    		mark=*,								% mark color
    		mark options={fill=gray,draw=black} 	% marker style
    ] coordinates {
    		(0, 1)
    		({pi/6},{sqrt(3)/2})
        ({pi/4},{sqrt(2)/2})
        ({pi/3},{1/2})
        ({pi/2},0)
        ({2*pi/3},{-1/2})
        ({3*pi/4},{-sqrt(2)/2})
        ({5*pi/6},{-sqrt(3)/2})
        ({pi},-1)
        ({7*pi/6},{-sqrt(3)/2})
        ({5*pi/4},{-sqrt(2)/2})
        ({4*pi/3},{-1/2})
        ({3*pi/2},0)
        ({5*pi/3},{1/2})
        ({7*pi/4},{sqrt(2)/2})
        ({11*pi/6},{sqrt(3)/2})
        ({2*pi},1)
        
    };

%X-Comb is defined
\addplot+ [
        xcomb,
        gray!50,                				% line color
    		mark=*,								% mark color
    		mark options={fill=gray,draw=black} 	% marker style
    ] coordinates {
        ({2*pi}, 1)
        ({11*pi/6}, {sqrt(3)/2})
        ({7*pi/4}, {sqrt(2)/2})
        ({5*pi/3}, {1/2})
        ({4*pi/3}, {-1/2})
        ({5*pi/4}, {-sqrt(2)/2})
        ({7*pi/6}, {-sqrt(3)/2})
        ({pi}, -1)
        
    };
\end{axis}
\end{tikzpicture}
\end{document}