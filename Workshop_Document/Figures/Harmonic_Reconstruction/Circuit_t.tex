\documentclass[../../main.tex]{subfiles}
\graphicspath{{\subfix{}}}

\begin{document}

\begin{circuitikz}[american,littleR/.style={R, resistors/scale=0.5}] %Usually used - left incase :)
\def\boxlength{3};

\ctikzset{sources/scale=1.0}
\draw (0, 0) to [V, invert, l=${v_i(t)}$] (0, \boxlength)
			to [R, l=${R}$] (\boxlength, \boxlength)
			to [C, l_=${C}$, v^=${v_o(t)}$] (\boxlength, 0)
			to [short] (0, 0);
			
\draw (0, 0) node[sground]{};


\end{circuitikz}

\end{document}